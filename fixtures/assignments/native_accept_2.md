# Native acceptance child 2

WORKTREE=/absolute/path/to/a/safe/test/repository

Nonce: NATIVE-GLM-ACCEPT-2

Read-only. Echo the nonce, use native shell tools to run `pwd`, `git status --short --branch`, `bun test tests/static.test.mjs`, and `docker version --format '{{json .}}'`, then report visible model/provider/role metadata. Do not edit, stage, commit, push, or call `codex exec`.
